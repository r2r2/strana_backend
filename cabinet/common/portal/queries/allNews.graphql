query allNews{
    allNews(
        kind: "action",
        first: 16,
        order: "order",
        projectsCity: %s
    ) {
        edges {
            node {
                id
                title
                end
                slug
                cardImageDisplay
            }
        }
    }
}