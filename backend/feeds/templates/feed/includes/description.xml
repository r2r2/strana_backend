{% if object.description %}
      {{ object.description }}
          {% else %}
          Коммерческое помещение № {{ object.id }} площадью {{ object.area }} м в ЖК {{ object.building.project.name }}
      {% endif %}
